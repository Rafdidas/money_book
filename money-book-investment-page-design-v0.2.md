# Money Book 투자 페이지 설계서 v0.2

## 1. 문서 목적

본 문서는 Money Book 가계부 서비스의 **투자 페이지** 기능을 설계하기 위한 문서이다.

투자 페이지는 사용자가 직접 입력한 보유 종목 정보를 기준으로 투자 현황을 기록하고, 현재가 조회를 통해 평가금액, 평가손익, 수익률, 종목별 비중, 계좌별 투자 현황을 확인할 수 있도록 한다.

현재 단계에서는 실제 증권 계좌 연동, 주문, 매매, 호가, 실시간 차트 기능은 포함하지 않는다.

---

## 2. 기능 방향

### 2.1 핵심 목적

투자 페이지의 목적은 다음과 같다.

- 사용자가 매수 종목, 매수단가, 수량을 직접 기록한다.
- 현재가 대비 평가금액과 수익률을 확인한다.
- 종목별 비중, 평가금액, 총 손익률, 자산 배분을 확인한다.
- ISA, 연금저축, 일반계좌 등 계좌 구분별 투자 현황을 확인한다.
- 기존 가계부의 월별 투자금 기록과는 별도로, 상세 투자 현황을 관리한다.

### 2.2 서비스 성격

이 기능은 증권 거래 서비스가 아니라 **사용자 입력 기반 투자 기록 및 평가 화면**이다.

따라서 다음 기능은 제공하지 않는다.

- 증권 계좌 잔고 자동 조회
- 주문 / 매수 / 매도 실행
- 호가창
- 투자자 매매 동향
- 실시간 차트
- 증권사 계좌 인증 기반 자산 연동

---

## 3. 현재 구현 현황

현재 Money Book에는 투자와 관련된 기능이 두 가지 흐름으로 존재한다.

1. 홈 / 분석 화면의 **투자원금 기록 기능**
2. `/app/invest` 투자 페이지의 **주식 보유내역 및 현재가 기반 평가 기능**

두 기능은 아직 데이터상 직접 연결되어 있지 않다.

---

## 4. 홈 / 분석 화면의 투자원금 기록 기능

### 4.1 현재 동작

홈 화면에서는 기존 가계부 입력 흐름 안에서 `투자` 항목을 기록할 수 있다.

현재 방식은 다음과 같다.

- 가계부 입력 유형에 `투자` 탭이 존재한다.
- 투자 카테고리는 `📈주식`이다.
- 투자 내역은 실제로 기존 `expense` 데이터 구조에 저장된다.
- 화면과 분석 로직에서 `📈주식` 카테고리를 투자원금으로 별도 분류한다.

### 4.2 안내 문구

현재 홈 화면에는 다음 안내 문구가 사용된다.

```txt
투자금은 현재 주가와 무관하게, 투자한 금액을 기록하기 위한 내역입니다.
```

### 4.3 분석 페이지 반영 항목

분석 페이지에서는 가계부 내역 중 카테고리에 `주식`이 포함된 지출을 투자금으로 분류한다.

현재 분석 화면에 반영되는 투자 항목은 다음과 같다.

- 월별 투자원금 합계
- 월별 투자 건수
- 연간 투자원금 합계
- 현금흐름 계산 시 투자원금 차감
- 월별 카드 내 투자원금 표시

### 4.4 분석 페이지에 반영되지 않는 항목

현재 분석 페이지에는 다음 항목이 반영되지 않는다.

- 투자 페이지의 보유 종목
- 현재 평가금액
- 평가손익
- 수익률
- 일간 수익

즉, 분석 페이지는 현재 **투자한 원금 기록**을 분석하고, 투자 페이지는 **보유 종목 평가**를 담당한다.

---

## 5. 투자 페이지 현재 구현 현황

현재 `/app/invest` 페이지에는 다음 기능이 구현되어 있다.

- 주식 종목 검색
- 매수일 입력
- 수량 입력
- 1주당 매입단가 입력
- 계좌 구분 입력
- KRW 통화값 저장
- 메모 입력
- 주식 보유내역 추가
- 동일 계좌 내 동일 종목의 여러 매수 기록 합산
- 현재가 조회
- 평가금액 계산
- 평가손익 및 수익률 계산
- 일간 등락 / 일간 손익 계산
- 전체 투자 요약 카드 표시
- 목록 중심 레이아웃 및 우측 종목 추가 패널
- 보유 종목 선택 상세 영역
- 개별 매수 기록 수정 / 삭제
- ISA / 연금저축 연간 한도 입력 및 사용률 카드
- 사용자별 마지막 성공 현재가 저장 및 재접속 시 복원
- 현재가 갱신 실패 시 마지막 조회값 안내 표시
- 종목별 정렬
- 데모 데이터 표시

### 5.1 현재 입력 항목

| 항목 | 구현 여부 | 비고 |
|---|---:|---|
| 종목명 | 구현 | 검색 결과에서 선택 |
| 종목코드 | 구현 | 종목 선택 시 함께 저장 |
| 시장 | 구현 | `KOSPI`, `KOSDAQ` |
| 매수일 | 구현 | 날짜 입력 |
| 수량 | 구현 | 숫자 입력 |
| 매입단가 | 구현 | 숫자 입력 |
| 계좌 구분 | 구현 | ISA / 연금저축 / 일반계좌 |
| 통화 | 구현 | MVP는 `KRW` 고정 저장 |
| 메모 | 구현 | 선택 입력 |

### 5.2 현재 저장 데이터 형태

현재 저장되는 주식 매수 데이터는 다음 구조를 기준으로 한다.

```ts
{
  id: string;
  createdAt: string;
  symbol: string;
  name: string;
  market: string;
  quantity: number;
  unitPrice: number;
  purchaseDate: string;
  accountType: "ISA" | "PENSION" | "GENERAL";
  currency: "KRW";
  memo: string;
}
```

### 5.3 현재 저장 방식

투자 페이지의 보유 종목은 현재 Supabase가 아니라 브라우저 `localStorage`에 저장된다.

저장 키 구조는 다음과 같다.

```txt
money-book:stock-holdings:{ownerKey}
```

`ownerKey`는 다음 기준이다.

- 데모 모드: `demo`
- 로그인 사용자: `displayEmail`
- 그 외: `local`

현재 저장 방식의 특징은 다음과 같다.

- 새로고침 후에도 같은 브라우저에서는 투자 내역이 유지된다.
- 로그인 사용자라도 다른 브라우저나 다른 기기와 동기화되지 않는다.
- Supabase 투자 전용 테이블은 아직 없다.
- 매수 기록의 추가 / 수정 / 삭제가 가능하다.
- 기존 저장 데이터는 읽을 때 `GENERAL` / `KRW` / 빈 메모 기본값으로 보정한다.
- ISA / 연금저축 한도는 사용자 및 연도별 `localStorage` 키로 저장한다.

---

## 6. 종목 검색 API 구현 현황

종목 검색은 `/api/stocks/search?q=검색어`로 호출된다.

현재 동작 방식은 다음과 같다.

- KOSPI 종목 마스터 파일 다운로드
- KOSDAQ 종목 마스터 파일 다운로드
- 압축 해제 후 종목명 / 종목코드 추출
- 이름 또는 코드로 검색
- 최대 12개 결과 반환
- 서버 메모리에 12시간 캐시

### 6.1 현재 지원 범위

| 범위 | 구현 여부 |
|---|---:|
| KOSPI | 구현 |
| KOSDAQ | 구현 |
| 국내 ETF | 종목 마스터에 포함되는 경우 검색 가능 |
| 미국주식 / 해외주식 | 미구현 |
| 사용자 직접 종목명 입력 | 미구현, 검색 선택 방식 |

종목 검색은 KIS 접근토큰 기반 현재가 호출과 달리, 종목 마스터 파일을 이용한다.

---

## 7. 현재가 API 구현 현황

현재가 조회 흐름은 다음과 같다.

```txt
투자 페이지
→ POST /api/stocks/quotes
→ 서버에서 KIS access token 발급 또는 재사용
→ KIS 국내주식 현재가 API 호출
→ 현재가 / 전일대비 / 등락률 반환
→ 화면에서 평가금액과 손익 계산
```

사용 환경변수는 다음과 같다.

```txt
KIS_APP_KEY
KIS_APP_SECRET
KIS_BASE_URL
```

현재가 응답 데이터는 다음 형태이다.

```ts
{
  symbol: string;
  currentPrice: number;
  dailyChange: number;
  dailyChangeRate: number;
  updatedAt: string;
}
```

### 7.1 현재 구현된 처리

- 국내주식 현재가 조회
- KIS access token 메모리 캐시
- 현재가 갱신 버튼
- 로그인 사용자에 대한 자동 현재가 조회
- 브라우저 기준 12시간 간격 자동 갱신 제한
- 데모 모드에서는 API 호출 대신 mock 현재가 사용
- 현재가 API의 서버 측 Supabase 로그인 검증
- 요청당 최대 20개 종목 조회 제한
- 마지막 성공 현재가의 사용자별 `localStorage` 저장 및 복원
- 마지막 현재가 조회 시각 표시

### 7.2 현재 구체적인 동작

1. 종목을 추가하면 해당 종목의 현재가를 즉시 조회한다.
2. 저장된 종목이 있으면 로그인 사용자에 대해 12시간 간격으로 자동 조회한다.
3. 사용자가 `정보 업데이트` 버튼을 누르면 보유 종목의 현재가를 다시 조회한다.
4. 서버는 여러 종목을 한 번에 받은 뒤 실제 KIS 요청은 종목별로 순차 실행한다.
5. 서버는 KIS access token을 메모리에 보관하고 만료 전까지 재사용한다.

---

## 8. 현재 투자 페이지 계산 로직

현재 목록은 `accountType + market + symbol + currency` 기준으로 합산된다.

예를 들어 같은 계좌의 삼성전자를 두 번 등록하면 하나의 행으로 합산된다.
일반계좌와 ISA에서 각각 삼성전자를 등록하면 계좌별로 서로 다른 행으로 표시된다.

### 8.1 구현된 계산값

| 계산값 | 구현 방식 |
|---|---|
| 총 매수금액 | `수량 × 매입단가` 합계 |
| 총 수량 | 동일 종목 매수 수량 합계 |
| 평균 매입가 | `총 매수금액 / 총 수량` |
| 현재 평가금액 | `현재가 × 총 수량` |
| 평가손익 | `현재 평가금액 - 총 매수금액` |
| 총 수익률 | `평가손익 / 총 매수금액 × 100` |
| 일간 손익 | `전일대비 금액 × 총 수량` |
| 일간 등락률 | API가 반환한 종목 등락률 |

### 8.2 현재 목록 컬럼

현재 목록 컬럼은 다음과 같다.

- 종목명
- 총 수익
- 1주 평균 금액
- 총 금액
- 일간 수익

표시 방식상 `총 금액` 셀 안에는 투자원금과 평가금액이 함께 표시된다.

---

## 9. 정렬 기능

현재 다음 항목별 정렬이 구현되어 있다.

- 종목명
- 총 수익
- 1주 평균 금액
- 총 금액
- 일간 수익

정렬 버튼은 다음 순서로 동작한다.

```txt
오름차순 → 내림차순 → 정렬 해제
```

---

## 10. 데모 모드 구현 현황

데모 모드에는 투자 화면용 샘플 데이터가 있다.

현재 샘플은 다음과 같다.

- 삼성전자 두 건의 매수 기록
- 삼성전자 mock 현재가
- mock 일간 등락 데이터

데모 투자 보유 데이터도 `localStorage`에 저장되며, 데모 모드를 끄면 관련 저장값이 제거된다.

추가로 홈 / 분석 화면용 데모 투자원금 내역도 별도로 생성된다.

---

## 11. 현재 API Key 동작 구조

현재 투자 페이지의 현재가 조회는 다음 방식이다.

```txt
사용자 브라우저
→ POST /api/stocks/quotes
→ Money Book 서버의 KIS_APP_KEY / KIS_APP_SECRET 사용
→ KIS access token 발급
→ KIS 국내주식 현재가 조회
→ 평가금액 / 수익률 계산
```

`KIS_APP_KEY`와 `KIS_APP_SECRET`은 서버 환경변수이고 `NEXT_PUBLIC_` 접두사가 없으므로, 현재 코드에서는 브라우저 번들에 직접 노출되지 않는다.

따라서 현재 구조의 핵심 문제는 **API Key가 브라우저에 노출되는 문제**가 아니라, 다음 두 가지이다.

1. 서버 API 엔드포인트 악용 가능성
2. KIS 시세 데이터를 제3자 사용자에게 표시할 수 있는지에 대한 이용 권한 문제

---

## 12. 현재 발생 가능한 동작 문제

| 문제 | 현재 상태 | 영향 |
|---|---|---|
| 배포 환경에 키가 없음 | `KIS_APP_KEY` / `KIS_APP_SECRET` 미설정 시 오류 | 현재가 조회 실패 |
| 공개 시세 활성화 게이트 | 프로덕션에서는 `KIS_PUBLIC_QUOTE_ENABLED=true`일 때만 현재가 조회 허용 | 승인 전 신규 KIS 현재가 조회 차단 |
| 공용 키 사용 | 서버 키 하나로 모든 사용자 요청 처리 | 호출량 / 장애가 전체 사용자에게 공유됨 |
| 서버 인증 | `/api/stocks/quotes`가 Supabase 로그인 사용자를 검증함 | 비로그인 직접 호출 차단 |
| 요청 종목 수 제한 | 한 요청당 최대 20개 종목 허용 | 단일 요청의 외부 API 호출량 제한 |
| 사용자별 rate limit | 인증 사용자 기준 인스턴스당 1분 5회 제한 적용 | 반복 호출 일부 차단, 다중 인스턴스 공유 제한은 미구현 |
| 시세 캐시 | 동일 인스턴스에서 종목별 2분 메모리 캐시 및 동시 요청 병합 적용 | KIS 호출량 감소, 서버리스 인스턴스 간 공유는 미구현 |
| 토큰 캐시가 메모리 전용 | `tokenCache` 변수에 저장 | 서버리스 인스턴스마다 토큰 재발급 가능 |
| 갱신 시간 키 | `money-book-stock-last-refresh:{ownerKey}` 기준으로 사용자별 저장 | 동일 브라우저 사용자 전환 시 분리됨 |
| 마지막 성공 현재가 | `money-book:stock-quotes:{ownerKey}`에 저장 | 재접속 및 API 실패 시 마지막 조회값 표시 가능 |

---

## 13. 기술 문제와 이용 권한 문제의 분리

현재 구조는 기술적으로 동작할 수 있다.

사용자가 증권 계좌를 연결하지 않아도, 서버가 현재가만 조회하여 평가손익을 계산하는 구현은 가능하다.

그러나 공개 서비스로 운영할 때는 **서버의 단일 KIS Key로 제3자 사용자들에게 시세를 보여줘도 되는가**가 별도 문제이다.

KIS 공식 제휴 안내를 2026년 5월 27일 확인한 결과, 다음 조건이 안내되어 있다.

- Open API를 활용하여 제3자에게 서비스를 제공하려는 법인은 제휴 대상이다.
- 제휴 제안 등록 안내에는 고객 대상 서비스를 제공하는 제도권금융회사
  (투자일임업, 투자자문업)가 제휴 대상이며, 제도권금융회사가 아닌 경우
  제휴가 불가하다는 안내가 있다.
- 제휴 안내에는 법인이 제휴사 브랜드 앱 화면에서 시세정보를 이용하는 경우,
  코스콤과의 정보이용계약이 사전에 체결되지 않았다면 이용할 수 없다는 안내가 있다.
- 제휴사 API 안내에는 시세 정보 이용이 한국투자증권 거래고객 대상 Open API
  서비스에 한하여 허용된다고 명시되어 있다.

따라서 Money Book처럼 사용자가 KIS 거래고객 인증을 거치지 않고 서버의 공용
Key로 현재가를 보여주는 구조는, “현재가만 보여주는 기능”이어도 승인 없이
외부 사용자 대상 공개 배포에 활성화하지 않는다. Money Book 운영 주체가
제도권금융회사 요건에 해당하지 않는다면, 공개 운영용 시세는 KIS 제휴가 아닌
별도 이용 허용 범위를 제공하는 데이터 공급자를 우선 검토한다.

공식 확인 출처:

- KIS Developers 제휴안내: <https://apiportal.koreainvestment.com/provider>
- KIS Developers 제휴 제안 등록: <https://apiportal.koreainvestment.com/provider-apply>
- KIS Developers 제휴사 API 시작하기: <https://apiportal.koreainvestment.com/provider-doc1>

### 13.1 운영 단계별 조건

현재가 API 사용 범위는 운영 단계에 따라 구분한다.

| 운영 단계 | 현재가 API 처리 |
|---|---|
| 로컬 개발 / 데모 검증 | 기존 KIS 연동 또는 mock 데이터를 사용해 화면 개발 가능 |
| 제한된 내부 테스트 | 서버 인증과 요청 종목 수 제한을 우선 적용한 뒤 테스트 |
| 외부 사용자 대상 공개 배포 | 제휴 / 정보이용계약 등 시세 표시 승인 확인 후 `KIS_PUBLIC_QUOTE_ENABLED=true`인 경우에만 제공 |

KIS 시세 표시 허용 범위 확인은 후속 편의 기능이 아니라, 외부 사용자 대상
공개 배포 여부를 결정하는 선행 조건으로 관리한다.

---

## 14. MVP 범위

### 14.1 MVP 포함 기능

1차 MVP에서는 다음 기능을 우선 구현한다.

- 투자 종목 등록
- 투자 종목 수정
- 투자 종목 삭제
- 보유 종목 목록 조회
- 종목별 평가 정보 표시
- 전체 투자 요약 카드 표시
- 계좌 구분 관리
- ISA / 연금저축 한도 입력 및 표시
- 포트폴리오 비중 표시
- 기존 현재가 API 연동 유지
- 현재가 API 호출 보안 보강

MVP의 시세 조회 및 합산 지원 범위는 다음과 같이 제한한다.

```txt
시장: 국내주식, 국내 ETF
통화: KRW 고정
```

현재 KIS 구현이 국내 종목 현재가 조회를 기준으로 하고 있으며, 환율 없이 다른
통화의 평가금액을 전체 합계에 포함할 수 없으므로 해외주식과 USD 입력은 MVP
화면에서 제공하지 않는다.

### 14.2 MVP에서 제외하는 기능

다음 기능은 MVP에서 제외한다.

- 사용자별 KIS 계좌 인증
- 주문 / 매매 기능
- 실시간 차트
- 호가창
- 배당금 기록
- 매도 기록
- 실현손익 계산
- 환율 자동 반영
- 해외주식 현재가 자동 조회
- 해외주식 / USD 직접 입력 및 평가금액 합산

---

## 15. 입력 데이터 설계

MVP에서 사용자가 직접 입력하는 투자 데이터는 다음과 같다.

| 항목 | 설명 | 필수 여부 |
|---|---|---|
| 시장 구분 | 국내주식, 국내 ETF | 필수 |
| 종목명 | 사용자가 입력하거나 검색으로 선택한 종목명 | 필수 |
| 종목코드 | 국내 종목코드 | 필수 |
| 매수일 | 해당 종목을 매수한 날짜 | 필수 |
| 매수단가 | 1주당 매수 가격 | 필수 |
| 수량 | 보유 수량 | 필수 |
| 통화 | KRW 고정값 | 필수 |
| 계좌 구분 | ISA, 연금저축, 일반계좌 | 필수 |
| 메모 | 사용자가 남기는 참고 메모 | 선택 |

### 15.1 시장 구분

```txt
DOMESTIC_STOCK: 국내주식
DOMESTIC_ETF: 국내 ETF
```

`OVERSEAS_STOCK`, `ETC`는 해외 현재가 및 환율 처리 정책을 확정한 뒤
추가 검토한다.

### 15.2 계좌 구분

```txt
ISA: ISA
PENSION: 연금저축
GENERAL: 일반계좌
```

### 15.3 통화 구분

```txt
KRW: 원화
```

`USD`는 해외주식 현재가와 환율 합산 정책을 확정한 뒤 추가한다.

---

## 16. ISA / 연금저축 한도 관리

투자 페이지에서는 ISA와 연금저축 계좌의 한도를 별도로 관리할 수 있도록 한다.

### 16.1 목적

- 사용자가 계좌별 투자 가능 한도를 확인할 수 있다.
- ISA / 연금저축 계좌에 어느 정도 투자했는지 파악할 수 있다.
- 추후 절세 계좌 관리 기능으로 확장할 수 있다.

### 16.2 관리 항목

| 항목 | 설명 |
|---|---|
| 계좌 구분 | ISA / 연금저축 |
| 연간 한도 | 사용자가 직접 입력 |
| 현재 투자금 | 해당 계좌에 등록된 매수금액 합계 |
| 남은 한도 | 연간 한도 - 현재 투자금 |
| 사용률 | 현재 투자금 / 연간 한도 × 100 |

### 16.3 MVP 처리 방식

- ISA / 연금저축 한도는 사용자가 직접 입력한다.
- 현재 연도에 등록된 해당 계좌 매수금액 합계를 현재 투자금으로 계산한다.
- 연간 한도와 사용률 카드는 투자 요약 영역 아래에 표시한다.
- 세법 기준 한도를 자동으로 강제하지 않는다.
- 안내 문구를 함께 표시한다.

```txt
ISA 및 연금저축 한도는 사용자가 직접 입력한 값을 기준으로 계산됩니다.
실제 세제 혜택 한도와 다를 수 있으므로 참고용으로만 확인해주세요.
```

---

## 17. 계산 로직

### 17.1 매수 기록 기준 계산

| 계산값 | 공식 |
|---|---|
| 매수금액 | `매수단가 × 수량` |
| 현재 평가금액 | `현재가 × 수량` |
| 평가손익 | `현재 평가금액 - 매수금액` |
| 수익률 | `평가손익 / 매수금액 × 100` |

### 17.2 보유 목록 집계 계산

매수 기록은 각 매수 건별로 별도 저장한다.

보유 종목 목록은 동일한 계좌 안의 동일 종목을 묶어 보여주며, MVP의 기본
집계 키는 다음과 같다.

```txt
accountType + market + symbol + currency
```

MVP에서는 `currency`가 `KRW`로 고정되므로 실제 화면 집계는
`accountType + market + symbol` 기준으로 처리할 수 있다.

같은 삼성전자 종목이라도 일반계좌와 ISA에 나누어 보유한 경우에는 목록에서
서로 다른 행으로 표시한다. 이를 통해 계좌별 투자금, 한도 사용률 및 계좌별
비중 계산을 일관되게 처리한다.

| 계산값 | 공식 |
|---|---|
| 총 수량 | 동일 계좌/종목의 매수 수량 합계 |
| 총 매수금액 | 동일 계좌/종목의 매수금액 합계 |
| 평균 매입가 | `총 매수금액 / 총 수량` |
| 현재 평가금액 | `현재가 × 총 수량` |
| 평가손익 | `현재 평가금액 - 총 매수금액` |
| 수익률 | `평가손익 / 총 매수금액 × 100` |
| 일간 수익 | `전일대비 금액 × 총 수량` |

### 17.3 종목별 비중 집계

포트폴리오의 종목별 비중을 표시할 때는 계좌가 다른 동일 종목 보유행을 다시
합산할 수 있다.

```txt
market + symbol + currency
```

예를 들어 삼성전자를 일반계좌와 ISA에서 각각 보유해도 `삼성전자 비중`은 두
계좌의 평가금액을 합친 값으로 표시한다.

### 17.4 전체 투자 요약 계산

| 계산값 | 설명 |
|---|---|
| 총 투자금액 | 모든 종목의 총 매수금액 합계 |
| 총 평가금액 | 모든 종목의 현재 평가금액 합계 |
| 총수익 | 총 평가금액 - 총 투자금액 |
| 총 수익률 | 총수익 / 총 투자금액 × 100 |
| 일간 수익 | 모든 종목의 일간 손익 합계 |
| 종목별 비중 | 종목 평가금액 / 총 평가금액 × 100 |
| 계좌별 비중 | 계좌별 평가금액 / 총 평가금액 × 100 |
| 시장별 비중 | 시장별 평가금액 / 총 평가금액 × 100 |
| 통화별 비중 | 통화별 평가금액 / 총 평가금액 × 100 |

---

## 18. 현재가 API 처리 정책

MVP에서는 이미 구현된 현재가 API 연동을 유지한다.

### 18.1 기본 정책

- 일반 사용자 화면에서는 서버 라우트를 통해 조회한 현재가로 평가금액과 수익률을 계산한다.
- 데모 모드에서는 기존 mock 현재가를 유지하여 외부 API 호출 없이 화면을 확인할 수 있도록 한다.
- 현재가 조회에 실패한 경우 마지막으로 성공한 값이 있다면 유지한다.
- 마지막 성공 값이 없다면 평가 정보가 갱신되지 않았음을 안내한다.
- 현재가는 참고용 값이며 실제 증권사 계좌 평가금액과 차이가 있을 수 있음을 표시한다.

### 18.2 API 조회 실패 문구

```txt
현재가를 불러오지 못했습니다.
잠시 후 다시 시도해주세요.
마지막으로 조회된 값이 있는 경우 해당 값을 기준으로 표시합니다.
```

### 18.3 현재가 안내 문구

```txt
현재가는 참고용으로 제공되며, 실제 증권사 계좌 평가금액과 차이가 있을 수 있습니다.
```

---

## 19. 화면 구성

투자 페이지는 **목록 중심의 포트폴리오 평가 화면**으로 구성한다.

### 19.1 전체 레이아웃

```txt
투자 페이지
├─ 상단 요약 카드 영역
│  ├─ 총 평가금액
│  ├─ 총 투자금액
│  ├─ 총수익
│  ├─ 총 수익률
│  └─ 일간 수익
│
├─ 계좌 한도 요약 영역
│  ├─ ISA 한도 사용률
│  └─ 연금저축 한도 사용률
│
├─ 포트폴리오 비중 영역
│  ├─ 종목별 비중
│  ├─ 계좌별 비중
│  ├─ 시장별 비중
│  └─ 통화별 비중
│
├─ 보유 종목 목록 영역
│  ├─ 종목명
│  ├─ 시장 구분
│  ├─ 계좌 구분
│  ├─ 수량
│  ├─ 평균 매입가
│  ├─ 현재가
│  ├─ 평가금액
│  ├─ 평가손익
│  └─ 수익률
│
├─ 종목 상세 영역
│  ├─ 매수 기록
│  ├─ 가격 정보
│  └─ 메모
│
└─ 종목 추가 / 수정 패널
```

### 19.2 상단 요약 카드

상단에는 투자 현황을 빠르게 파악할 수 있는 요약 카드를 배치한다.

표시 항목은 다음과 같다.

- 총 평가금액
- 총 투자금액
- 총수익
- 총 수익률
- 일간 수익

### 19.3 계좌 한도 요약 영역

계좌 한도 요약 영역에는 다음 정보를 표시한다.

- ISA 연간 한도
- ISA 현재 투자금
- ISA 남은 한도
- ISA 사용률
- 연금저축 연간 한도
- 연금저축 현재 투자금
- 연금저축 남은 한도
- 연금저축 사용률

### 19.4 포트폴리오 비중 영역

포트폴리오 비중 영역에서는 다음 기준의 비중을 표시한다.

- 종목별 비중
- 계좌별 비중
- 시장별 비중
- 통화별 비중

초기 구현에서는 평가금액 기준 막대 그래프 카드로 표시한다. 종목별 비중은
계좌가 다른 동일 종목을 합산하고, 계좌별 / 시장별 / 통화별 배분은 각 분류의
평가금액 합계를 기준으로 계산한다. (완료)

### 19.5 보유 종목 목록

보유 종목 목록은 페이지의 핵심 영역이다.

표시 항목은 다음과 같다.

| 컬럼 | 설명 |
|---|---|
| 종목명 | 종목명 + 종목코드 |
| 시장 | 국내주식 / 해외주식 / ETF / 기타 |
| 계좌 | ISA / 연금저축 / 일반계좌 |
| 수량 | 보유 수량 |
| 평균 매입가 | 총 매수금액 / 총 수량 |
| 현재가 | 현재가 API 조회 값 |
| 평가금액 | 현재가 × 수량 |
| 평가손익 | 평가금액 - 총 매수금액 |
| 수익률 | 평가손익 / 총 매수금액 × 100 |

### 19.6 색상 규칙

국내 주식 UX에 맞춰 다음 색상 규칙을 적용한다.

- 상승 / 수익: 빨간색 계열
- 하락 / 손실: 파란색 계열
- 변화 없음: 기본 텍스트 색상

단, 접근성을 위해 색상만으로 상태를 구분하지 않고 `+`, `-`, `상승`, `하락` 등의 텍스트 정보를 함께 제공한다.

### 19.7 종목 상세 영역

종목 행을 클릭하면 상세 영역을 표시한다.

상세 영역에서는 다음 정보를 보여준다.

- 종목명
- 종목코드
- 시장 구분
- 계좌 구분
- 매수일
- 매수단가
- 수량
- 총 매수금액
- 현재가
- 평가금액
- 평가손익
- 수익률
- 메모

상세 영역의 상단에는 선택 종목의 현재가와 오늘 변동을 함께 표시한다. 오늘 변동은
`전일대비 금액 x 보유 수량`으로 계산한 손익 금액과 API의 전일 대비율을 함께
노출한다. 상단 요약 카드의 오늘 손익도 포트폴리오 전체의 전일 평가금액 대비
비율과 금액을 함께 표시한다. (완료)

### 19.8 종목 추가 / 수정 패널

종목 추가 폼은 페이지 중심에 항상 노출하지 않고, 버튼 클릭 시 우측 패널 또는 모달 형태로 제공한다.

권장 방식은 우측 패널이다.

```txt
[+ 종목 추가] 버튼 클릭
→ 우측 패널 오픈
→ 종목 정보 입력
→ 저장
→ 목록 갱신
```

수정 시에는 기존 데이터를 패널에 채운 뒤 저장한다.

```txt
종목 행의 [수정] 클릭
→ 우측 패널 오픈
→ 기존 매수 기록 표시
→ 값 수정
→ 저장
→ 목록 갱신
```

---

## 20. UX 참고 방향

### 20.1 참고할 요소

토스증권과 같은 목록 중심의 깔끔한 투자 화면을 참고한다.

반영하기 좋은 요소는 다음과 같다.

- 큰 숫자 중심의 요약 카드
- 종목별 간결한 목록
- 수익 / 손실 색상 구분
- 카드형 패널 구성
- 탭 스타일
- 종목 행 클릭 시 상세 정보 표시
- 수동 업데이트 버튼
- 마지막 업데이트 시각 표시

### 20.2 제외할 요소

현재 서비스 범위에서는 다음 요소를 제외한다.

- 주문하기
- 매수 / 매도 버튼
- 호가창
- 실시간 차트
- 투자자 매매 동향
- 증권사 수준의 상세 거래 화면

---

## 21. API 설계

현재가 API는 기존 구현을 유지하여 MVP에서 사용한다.

단, 자동 조회를 계속 제공하기 전에 서버 라우트 보호와 호출량 제어를 함께 보강한다.

### 21.1 현재가 API 처리 방향

초기 구조는 다음과 같다.

```txt
브라우저
→ /api/stocks/quotes
→ Next.js 서버 라우트
→ 현재가 데이터 공급자 API
→ 현재가 반환
```

KIS API Key와 Secret은 브라우저에 노출하지 않고 서버 환경변수로만 관리한다.

```txt
KIS_APP_KEY
KIS_APP_SECRET
KIS_BASE_URL
```

위 값은 `NEXT_PUBLIC_` 접두사를 붙이지 않는다.

### 21.2 `/api/stocks/quotes` 요청 예시

```ts
type StockQuoteRequest = {
  symbols: Array<{
    symbol: string;
    market?: string;
  }>;
};
```

### 21.3 `/api/stocks/quotes` 응답 예시

```ts
type StockQuoteResponse = {
  quotes: Array<{
    symbol: string;
    currentPrice: number;
    dailyChange: number;
    dailyChangeRate: number;
    updatedAt: string;
  }>;
};
```

### 21.4 서버 측 검증 정책

`/api/stocks/quotes`는 반드시 서버 측 로그인 검증을 수행해야 한다.

필수 보안 조건은 다음과 같다.

- Supabase 서버 인증으로 로그인 사용자 확인
- 비로그인 사용자의 직접 API 호출 차단
- 한 번에 조회 가능한 종목 개수 제한
- 사용자별 또는 IP별 호출 제한
- 동일 종목 현재가의 짧은 캐시 적용
- API Key / Secret은 서버 전용 환경변수로 유지

### 21.5 요청당 종목 수 제한

초기 제한값은 다음과 같이 둔다.

```txt
한 요청당 최대 20종목
```

초기 개인 서비스 규모에서는 20종목 정도면 충분하며, 이후 사용량에 따라 조정한다.

### 21.6 캐시 정책

동일 종목 현재가에 대해 짧은 캐시를 적용했다.

현재 적용 기준은 다음과 같다.

```txt
국내주식 현재가 캐시: 서버 인스턴스 메모리 기준 2분
동일 종목 동시 요청: 진행 중 Promise를 공유하여 중복 호출 방지
```

목적은 다음과 같다.

- 동일 종목 반복 조회 방지
- 여러 사용자가 같은 종목을 조회할 때 외부 API 호출량 감소
- 정보 업데이트 버튼 반복 클릭 시 호출량 보호

이 캐시는 실행 중인 서버 인스턴스 안에서만 공유된다. 서버리스 배포에서 여러 인스턴스가 생성되면 인스턴스 간 캐시는 공유되지 않으므로, 사용량이 커지는 공개 운영 단계에서는 공유 저장소 기반 캐시를 검토한다.

### 21.7 rate limit 정책

현재 적용 기준은 다음과 같다.

```txt
인증 사용자 기준: 서버 인스턴스당 1분 5회
초과 응답: HTTP 429 + Retry-After 헤더
```

현재 제한은 `user.id` 기준으로 적용되며 IP 기준 제한은 아직 적용하지 않는다. 이 제한 역시 단일 서버 인스턴스 기준이므로, 다중 인스턴스에서 동일한 제한을 강제해야 하는 시점에는 공유 저장소 기반 rate limit으로 확장한다.

### 21.8 API 조회 실패 처리

현재가 API 호출이 실패하거나 일시적으로 제한된 상태에서는 다음 흐름을 적용한다.

1. 마지막 성공 현재가가 있으면 해당 값으로 평가정보를 유지한다.
2. 마지막 성공 현재가가 없으면 현재가 관련 계산값을 비활성 또는 `-`로 표시한다.
3. 사용자에게 현재가 조회 실패 안내를 표시한다.
4. 재시도는 사용자가 직접 `정보 업데이트` 버튼을 누를 때 수행한다.

---

## 22. 현재가 API 및 API Key 운영 현황

현재 투자 페이지는 Next.js 서버 라우트 `/api/stocks/quotes`를 통해 한국투자증권 KIS 국내주식 현재가 API를 호출한다.

`KIS_APP_KEY`와 `KIS_APP_SECRET`은 서버 환경변수로 관리되며, 브라우저에는 직접 노출되지 않는다.

사용자는 종목과 매수 정보를 직접 입력하고, 서버에서 조회한 현재가를 기준으로 평가금액, 평가손익, 수익률을 계산한다.

현재 구현은 서버의 단일 KIS API Key를 모든 서비스 사용자가 공유하는 구조이다.

따라서 배포 환경에 키가 누락되면 현재가 조회가 실패하며, 서비스 사용자가 증가할 경우 동일 키의 호출량과 장애 영향이 전체 사용자에게 공유된다.

프로덕션 배포에서는 `KIS_PUBLIC_QUOTE_ENABLED=true`가 명시되지 않은 경우
`/api/stocks/quotes`가 `503`을 반환하도록 하여, 시세 표시 승인 확인 전에는
공개 서비스에서 신규 KIS 현재가 조회가 활성화되지 않도록 한다. 브라우저에
기존 마지막 성공값이 남아 있는 사용자에 대한 표시 정책은 공개 전 별도로
결정한다.

현재 화면과 `/api/stocks/quotes`에는 서버 측 로그인 검증, 요청당 최대 20개
종목 제한, 브라우저별 마지막 성공 현재가 저장 및 실패 fallback, 인스턴스 기준
사용자별 1분 5회 제한, 종목별 2분 현재가 캐시가 적용되어 있다.

따라서 한 서버 인스턴스 안에서의 반복 호출은 줄일 수 있다. 다만 서버리스 배포에서 여러 인스턴스가 병렬로 동작하면 캐시와 rate limit이 공유되지 않으므로, 공개 규모가 커질 때는 공유 저장소 기반 보호가 추가로 필요하다.

KIS 공식 제휴 안내 기준으로 제3자 대상 서비스에서 시세 정보를 앱 화면에
제공하려면 제휴 및 정보이용계약 조건을 충족해야 하므로, 공개 운영 전 KIS
또는 관련 데이터 공급자에게 현재 서비스 형태의 시세 표시 승인을 받아야 한다.

---

## 23. 저장 방식과 데이터베이스 설계 초안

### 23.1 MVP 저장 방식

현재 구현에는 브라우저 `localStorage` 기반 보유 종목 저장 흐름이 이미 존재한다.

MVP에서는 이 흐름을 유지하여 투자 화면과 계산 기능을 우선 완성한다.

- 보유 종목과 입력 내역은 기존 `localStorage` 저장 구조를 확장하여 관리한다.
- 데모 모드는 기존 초기 데이터 및 mock 현재가 구조를 유지한다.
- 로그인 사용자도 MVP에서는 동일 브라우저에 저장된 투자 기록을 사용한다.
- 여러 기기 동기화, 데이터 백업, 장기 보존이 필요해지는 시점에 Supabase 테이블 저장으로 이전한다.

`localStorage`는 브라우저 단위 저장이므로 기기 간 동기화와 서버 백업을 제공하지 않는다는 안내가 필요하다.

### 23.2 localStorage 저장 구조 확장안

기존 저장 데이터에 다음 필드를 추가한다.

```ts
type InvestmentHolding = {
  id: string;
  createdAt: string;
  updatedAt?: string;

  symbol: string;
  name: string;
  market: string;

  quantity: number;
  unitPrice: number;
  purchaseDate: string;

  accountType: "ISA" | "PENSION" | "GENERAL";
  currency: "KRW" | "USD";
  memo?: string;
};
```

### 23.3 마지막 성공 현재가 저장 구조

현재가 조회 실패 시 fallback을 위해 마지막 성공 현재가를 저장한다.

```ts
type StockQuoteCache = {
  market: string;
  symbol: string;
  currency: "KRW";
  currentPrice: number;
  dailyChange: number;
  dailyChangeRate: number;
  updatedAt: string;
};
```

저장 키 예시는 다음과 같다.

```txt
money-book:stock-quotes:{ownerKey}
```

### 23.4 계좌 한도 저장 구조

ISA / 연금저축 한도는 MVP에서 localStorage에 저장한다.

```ts
type InvestmentAccountLimit = {
  accountType: "ISA" | "PENSION";
  yearlyLimit: number;
  year: number;
  updatedAt: string;
};
```

저장 키 예시는 다음과 같다.

```txt
money-book:investment-account-limits:{ownerKey}:{year}
```

---

## 24. Supabase 데이터베이스 설계 초안

추후 로그인 사용자의 투자 내역 동기화가 필요할 때 Supabase 테이블로 이전한다.

MVP 필수 구현 범위는 아니다.

### 24.1 investment_holdings

| 컬럼 | 타입 예시 | 설명 |
|---|---|---|
| id | uuid | 고유 ID |
| user_id | uuid | 사용자 ID |
| market_type | text | 시장 구분 |
| account_type | text | 계좌 구분 |
| symbol | text | 종목코드 / 티커 |
| name | text | 종목명 |
| buy_date | date | 매수일 |
| buy_price | numeric | 매수단가 |
| quantity | numeric | 수량 |
| currency | text | KRW / USD |
| memo | text | 메모 |
| created_at | timestamptz | 생성일 |
| updated_at | timestamptz | 수정일 |

### 24.2 investment_account_limits

| 컬럼 | 타입 예시 | 설명 |
|---|---|---|
| id | uuid | 고유 ID |
| user_id | uuid | 사용자 ID |
| account_type | text | ISA / PENSION |
| yearly_limit | numeric | 연간 한도 |
| year | integer | 기준 연도 |
| created_at | timestamptz | 생성일 |
| updated_at | timestamptz | 수정일 |

### 24.3 stocks_quote_cache

현재가 API 연동 시 사용할 수 있는 서버 캐시 테이블이다.

| 컬럼 | 타입 예시 | 설명 |
|---|---|---|
| id | uuid | 고유 ID |
| market_type | text | 시장 구분 |
| symbol | text | 종목코드 / 티커 |
| current_price | numeric | 현재가 |
| previous_close | numeric | 전일 종가 |
| daily_change | numeric | 전일대비 |
| daily_change_rate | numeric | 등락률 |
| currency | text | 통화 |
| fetched_at | timestamptz | 조회 시각 |

해당 테이블은 MVP 필수는 아니며, API 호출량 또는 서버리스 인스턴스 간 캐시 공유가 필요할 때 추가한다.

---

## 25. 기존 가계부 기능과의 관계

현재 Money Book에는 월별 투자금 기록 기능이 존재한다.

투자 페이지는 기존 월별 투자금 기록과 바로 연결하지 않고, 우선 별도 상세 관리 기능으로 분리한다.

### 25.1 분리하는 이유

- 기존 투자금 기록은 월별 가계부 흐름을 위한 금액 기록이다.
- 투자 페이지는 종목별 보유 현황과 평가금액을 관리하는 기능이다.
- 현재가 반영 여부에 따라 금액이 계속 변할 수 있으므로 월별 가계부 기록과 직접 연결하면 혼란이 생길 수 있다.
- 가계부의 투자금은 현금흐름이고, 투자 페이지의 평가금액은 자산 평가값이다.

### 25.2 안내 문구

```txt
투자 페이지의 평가금액은 현재가 또는 입력값을 기준으로 계산됩니다.
기존 월별 투자금 기록은 실제 투자한 금액을 기록하기 위한 내역이며, 주가 변동을 반영하지 않습니다.
```

### 25.3 추후 확장 방향

추후에는 다음 방식으로 연결할 수 있다.

- 투자 페이지에서 등록한 매수금액을 월별 투자금 기록에 반영
- 계좌별 투자금 합계를 월별 대시보드에 표시
- 월별 순매수금액과 평가손익을 분리해서 표시
- 월별 현금흐름과 현재 자산 평가를 별도 카드로 구분 표시

---

## 26. 사용자 안내 문구

투자 페이지에는 사용자가 오해하지 않도록 다음 안내 문구를 제공한다.

### 26.1 투자 기록 안내

```txt
투자 내역은 사용자가 직접 입력한 기록을 기준으로 계산됩니다.
현재가는 참고용으로 제공되며, 실제 증권사 계좌 수익률과 차이가 있을 수 있습니다.
```

### 26.2 주문 기능 미제공 안내

```txt
이 페이지는 투자 기록 및 참고용 수익률 확인 기능을 제공합니다.
실제 매수, 매도, 주문 기능은 제공하지 않습니다.
```

### 26.3 ISA / 연금저축 안내

```txt
ISA 및 연금저축 한도는 사용자가 입력한 값을 기준으로 계산됩니다.
실제 세제 혜택 한도와 다를 수 있으므로 참고용으로 확인해주세요.
```

### 26.4 localStorage 저장 안내

```txt
현재 투자 내역은 사용 중인 브라우저에 저장됩니다.
다른 기기에서는 동일한 투자 내역이 보이지 않을 수 있습니다.
```

### 26.5 현재가 조회 실패 안내

```txt
현재가를 불러오지 못했습니다.
잠시 후 다시 시도해주세요.
마지막으로 조회된 값이 있는 경우 해당 값을 기준으로 표시합니다.
```

---

## 27. 개발 우선순위

### 27.1 1차 구현

- 투자 페이지 기본 레이아웃 구성
- 보유 종목 등록 / 수정 / 삭제 (완료)
- 보유 종목 목록 표시
- 총 투자금액 / 평가금액 / 총수익 / 수익률 계산
- 계좌 구분 추가
- 통화는 KRW 고정값으로 저장
- ISA / 연금저축 한도 입력 및 표시 (완료)
- 기존 현재가 API 조회 유지
- 데모 모드는 mock 현재가 유지
- 기존 `localStorage` 저장 구조에 계좌 / KRW 통화값 / 메모 / 한도 입력값 확장
- `/api/stocks/quotes` 서버 측 로그인 검증 추가 (완료)
- 한 번에 조회 가능한 종목 개수 제한 (완료: 최대 20개)

### 27.2 2차 구현

- 포트폴리오 비중 차트 추가 (완료: 평가금액 기준 막대 그래프 카드)
- 종목 상세 패널 추가 (완료: 보유 요약 및 개별 매수 기록)
- 시장별 / 계좌별 자산 배분 표시 (완료: 통화별 배분 포함)
- 일간 수익 표시 구조 개선 (완료: 전체 전일 대비율 및 선택 종목 오늘 변동 표시)
- 현재가 조회 실패 상태 및 마지막 조회값 표시 개선 (완료)
- 사용자별 rate limit 추가 (완료: 인스턴스 기준 사용자당 1분 5회)
- 현재가 짧은 캐시 추가 (완료: 인스턴스 기준 종목별 2분)
- 자동 갱신 시간 키를 사용자별로 분리 (완료)

### 27.3 공개 배포 전 완료 조건

- KIS 공식 제휴 / 시세 이용 안내 확인 (완료: 제3자 화면 제공은 승인 전 활성화 금지)
- KIS 또는 적법한 데이터 공급자의 시세 표시 승인 확보
- 필요 시 대체 데이터 공급자 검토
- `/api/stocks/quotes` 서버 측 로그인 검증 및 요청 제한 완료
- 사용자별 rate limit 적용 (완료: 인스턴스 기준 사용자당 1분 5회)
- 동일 종목 현재가의 짧은 캐시 적용 (완료: 인스턴스 기준 종목별 2분)
- 다중 인스턴스 운영 시 공유 저장소 기반 rate limit / 캐시 적용 여부 결정
- 현재가 실패 시 마지막 성공값 표시 적용 (완료)
- 프로덕션 현재가 활성화 게이트 적용 (완료: `KIS_PUBLIC_QUOTE_ENABLED=true` 필요)
- 승인 전 공개 시 기존 브라우저 저장 현재가를 숨길지 여부 결정

### 27.4 후속 구현

- 해외주식 현재가 및 환율 처리 검토
- Supabase 투자 테이블 이전 여부 검토

### 27.5 장기 확장

- 배당금 기록
- 매도 기록
- 실현손익 계산
- 월별 투자금 기록과 연동
- 환율 자동 반영
- 계좌별 투자 성과 분석
- 기기 간 동기화
- 백업 / 복원 기능

---

## 28. API 보안 보강 작업

현재 API 연동을 유지할 경우 설계서에 반드시 포함할 보완 사항이다.

### 28.1 필수 보완

- `/api/stocks/quotes` 서버 측 로그인 검증 (완료)
- 비로그인 요청 차단 (완료)
- 요청당 종목 수 상한 설정 (완료: 최대 20개)
- 사용자별 호출 제한 (완료: 인스턴스 기준 사용자당 1분 5회)
- 현재가 데이터의 서버 캐시 적용 (완료: 인스턴스 기준 종목별 2분)
- 현재가 조회 실패 시 마지막 성공값 fallback (완료: 브라우저 저장값 기준)
- 배포 환경의 KIS 환경변수 설정 점검
- 프로덕션 승인 확인 전 현재가 비활성화 게이트 (완료: `KIS_PUBLIC_QUOTE_ENABLED`)

### 28.2 권장 보완

- 서버리스 인스턴스 간 공유 가능한 캐시 구조 검토
- 다중 인스턴스 간 공유 가능한 rate limit 구조 검토
- KIS access token 캐시를 메모리 외 저장소로 확장 검토
- API 호출 실패 로그 수집
- 현재가 조회 실패율 모니터링
- 반복 호출 사용자 탐지
- 정보 업데이트 버튼 연속 클릭 방지

---

## 29. 오픈 이슈

추가 판단이 필요한 사항은 다음과 같다.

1. KIS 승인 절차를 진행할지, 시세 표시 권한을 제공하는 대체 데이터 공급자를 선택할지
2. KIS 또는 대체 공급자의 시세 표시 승인과 계약을 언제 확보할지
3. 해외주식 현재가와 환율을 어떻게 처리할 것인지
4. 동일 종목을 여러 번 매수했을 때 하나의 종목으로 합산할지, 매수 기록별로 분리할지
5. ISA / 연금저축 한도 기준을 사용자가 직접 입력하게 할지, 기본값을 제공할지
6. 투자 페이지의 금액을 기존 월별 투자금 기록과 언제 연결할지
7. 배당금과 매도 기록을 어느 시점에 추가할지
8. 투자 데이터를 언제 Supabase로 이전할지
9. 현재가 자동 조회를 계속 유지할지, 수동 업데이트 중심으로 전환할지
10. 승인 확보 전 공개 환경에 남아 있을 수 있는 브라우저 저장 현재가를 숨길지

---

## 30. 현재 결론

투자 페이지는 초기 단계에서 **사용자 입력 기반 투자 기록 + 포트폴리오 평가 화면**으로 구현한다.

기존 KIS 현재가 API 연동은 로컬 개발 및 승인된 운영 범위에서 유지할 수 있다. 현재 서버 측 인증, 요청당 종목 수 제한, 마지막 성공 현재가 저장 및 fallback, 인스턴스 기준 사용자별 rate limit과 현재가 캐시, 프로덕션 활성화 게이트가 적용되어 있다. 공식 안내상 제3자 화면의 시세 제공에는 제휴 및 정보이용계약 조건이 있으므로, 실제 승인을 확보하기 전 공개 환경에서 신규 KIS 현재가 조회는 활성화하지 않는다. 이후 다중 인스턴스 운영 규모가 되면 공유 rate limit과 캐시가 필요한지 판단한다.

기존 월별 투자금 기록은 유지하고, 투자 페이지는 기존 `localStorage` 기반 상세 관리 기능으로 먼저 운영한다.

여러 기기 동기화 또는 백업 요구가 생기면 Supabase 저장 구조로 이전한다.

서비스 사용량과 정책 확인 결과에 따라 현재가 자동 연동, 배당금, 매도 기록, 월별 투자금 연동 기능을 단계적으로 확장한다.

현재 설계에서 가장 중요한 구분은 다음과 같다.

```txt
가계부 투자금 기록 = 실제 투자한 원금 / 현금흐름
투자 페이지 평가금액 = 현재가 기반 자산 평가 / 참고용 수익률
```

따라서 두 기능은 초기에 분리 운영하고, 추후 사용자가 혼동하지 않는 방식으로 단계적으로 연결한다.
